<%= provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
	<%= gravatar_for @user %>
      </h1>
    </section>
  </aside>
<div class="span8">
    <h2> <%= @user.dares.count %> Dares </h2>
  <% if @user.dares.any? %>
    <ol class="dares">
      <%= render @dares %>
    </ol>
    <%= will_paginate @dares %>
    <% end %>
  </div>
</div>
